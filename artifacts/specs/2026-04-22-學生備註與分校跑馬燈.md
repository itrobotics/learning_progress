# 2026-04-22 變更摘要：學生備註欄 & 分校跑馬燈

## 一、學生資料新增備註欄

### 前端
- `react-app/src/components/StudentManageModal.jsx`
  - 基本資料區塊新增「備註」輸入欄（textarea）。
  - 新增/編輯時會帶入既有備註並可儲存。

- `react-app/src/components/StudentDetailPanel.jsx`
  - 學生資訊列中，在「速度 / 每週時程」下一行新增「備註」顯示。
  - 僅當備註有內容時顯示，避免空白行。

### 後端（Google Apps Script）
- `google_web.gs`
  - `STUDENT_REQUIRED_COLUMNS` 新增 `note`，會自動補齊學生設定欄位。
  - `buildStudentPayload_()` 新增 `note`，支援新增與更新。
  - `getStudents()` 讀取時回傳 `note`。

---

## 二、系統全域設定新增 3 分校跑馬燈訊息 + 開關

### 新增設定鍵
- 延壽：`marqueeMsgYanShou`、`marqueeEnabledYanShou`
- 安和：`marqueeMsgAnHe`、`marqueeEnabledAnHe`
- 大直：`marqueeMsgDaZhi`、`marqueeEnabledDaZhi`

### 後端（Google Apps Script）
- `google_web.gs`
  - `DEFAULT_SETTINGS` 新增上述 6 個鍵。
  - `normalizeSettingKey_()` 新增對應映射。
  - `normalizeSettingValue_()` 新增開關布林值正規化（接受 true/false、1/0、yes/no）。

### 前端
- `react-app/src/constants.js`
  - `APP_SETTINGS_DEFAULT` 新增跑馬燈訊息與開關預設值。

- `react-app/src/components/SettingsModal.jsx`
  - 系統全域設定新增三組分校「訊息內容 + 啟用 checkbox」。

- `react-app/src/App.jsx`
  - `applySystemSettings()` 新增跑馬燈設定正規化。
  - 依 `currentBranch` 選取對應跑馬燈設定。
  - 僅在「該分校啟用且訊息非空」時顯示跑馬燈。

- `react-app/src/App.css`
  - 新增跑馬燈樣式與動畫（`branch-marquee-wrap` / `branch-marquee-track`）。

---

## 三、顯示規則確認
- 切換分校顯示不同訊息。
- 跑馬燈是否顯示 **完全由系統全域設定開關決定**。

---

## 四、跑馬燈功能擴充：顏色與速度自訂

### 新增設定鍵
- 延壽：`marqueeColorYanShou`（預設 `#166534`）、`marqueeSpeedYanShou`（預設 `16` 秒）
- 安和：`marqueeColorAnHe`（預設 `#166534`）、`marqueeSpeedAnHe`（預設 `16` 秒）
- 大直：`marqueeColorDaZhi`（預設 `#166534`）、`marqueeSpeedDaZhi`（預設 `16` 秒）

### 後端（Google Apps Script）
- `google_web.gs`
  - `DEFAULT_SETTINGS` 新增上述 6 個鍵。
  - `normalizeSettingKey_()` 新增對應映射（marqueecoloryanshou → marqueeColorYanShou 等）。

### 前端
- `react-app/src/constants.js`
  - `APP_SETTINGS_DEFAULT` 新增顏色與速度預設值。

- `react-app/src/components/SettingsModal.jsx`
  - 三分校各新增「顏色」與「速度（秒）」輸入欄位。

- `react-app/src/App.jsx`
  - `applySystemSettings()` 新增 color/speed 解析。
  - `currentMarquee` useMemo 新增 color/speed 屬性。
  - 跑馬燈渲染套用 inline style（color 與 animationDuration）。

- `react-app/src/App.css`
  - 跑馬燈訊息間距從 `48px` 增加至 `80px`。

---

## 五、學生編輯驗證調整

### 前端
- `react-app/src/components/StudentManageModal.jsx`
  - 僅在新增學生（`!isEdit`）時檢查剩餘學習時數不可為負。
  - 編輯學生時允許剩餘學習時數為負值（應對特殊情況）。

```javascript
if (!isEdit && (payload.currentRemainingHours < 0 || payload.initHours < 0)) {
  setErrorMsg('時數不可為負數')
  return
}
```

---

## 六、驗證
- 指令：`npm run build --prefix "d:\ai_project\MPM學習進度\learning_progress\react-app"`
- 結果：✅ 成功（31 modules transformed, built in 155ms）
